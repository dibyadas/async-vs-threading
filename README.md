### Async vs Threading approach

I show a simple example of how we can use async and threading to accomplish a task and compare them based on CPU usage and code maintainability. To read from the event device file, you might need to set appropiate permissions first. You can also instead use a socket connection as the file descriptor.